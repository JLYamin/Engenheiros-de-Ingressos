<% if user_signed_in? %>
<% if  current_user.adm == true %>

<p id="notice"><%= notice %></p>

<h1>Users</h1>

<table>
  <thead>
    <tr>
      <th>nome</th>
      <th>email</th>
      <th>adm?</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
     <% @user.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= user.adm %></td>
         <td><%= link_to 'Show', user %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to "Mostrar users", users_path %>

<% else %>
  <p>Somente Adm</p>
  <a href="/">Voltar<a>
<% end %>
<% else%>
  <p>Somente Adm</p>
  <a href="/">Voltar<a>
<% end %>